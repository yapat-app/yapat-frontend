declare module "react-audio-spectrogram-player" {
  // Re‑export the library’s own types so you still get intellisense
  export * from "react-audio-spectrogram-player/dist/lib";
  const defaultExport: any; // or a more specific type if you inspect the .d.ts
  export default defaultExport;
}
